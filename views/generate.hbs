<div class="container">
    <div class="d-flex justify-content-center">
        <h6>Success! Please use this link:</h6>
    </div>
    <div class="d-flex justify-content-center">
        <a href="{{fullUrl}}"><small>{{newUrl}}</small></a>
    </div>
    {{!-- <div class="mt-2 d-flex justify-content-center">
        <button type="button" class="btn btn-dark btn-sm"><i class="far fa-copy ml-1 mr-1"></i>Copy</button>
    </div> --}}
    <div class="mt-3 d-flex justify-content-center">
        <h6>Original URL:</h6>
    </div>
    <div class="d-flex justify-content-center">
        <a href="{{fullUrl}}"><small>{{fullUrl}}</small></a>
    </div>
</div>